---
phase: 06-advanced-features-admin-interface
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [src/services/pricingService.js, src/services/relay/vertexRelayService.js]
autonomous: true
requirements: [FEATURES-02, FEATURES-03]
must_haves:
  truths:
    - "Vertex AI requests generate accurate usage statistics with input and output token counts"
    - "Cost calculations work correctly for Vertex AI usage based on model pricing"
    - "Usage data integrates with existing pricing service and statistics system"
  artifacts:
    - path: "src/services/pricingService.js"
      provides: "Vertex AI model pricing support"
      contains: "getModelPricing.*vertex"
      min_lines: 10
    - path: "src/services/relay/vertexRelayService.js"
      provides: "Usage capture and cost calculation"
      contains: "calculateCost.*usage.*model"
      min_lines: 20
  key_links:
    - from: "vertexRelayService"
      to: "pricingService.calculateCost"
      via: "usage cost calculation"
      pattern: "pricingService\\.calculateCost"
    - from: "vertexRelayService"
      to: "usage statistics"
      via: "token accumulation"
      pattern: "input_tokens.*output_tokens"
---

<objective>
Integrate Vertex AI usage statistics capture and cost calculation with existing pricing service.

Purpose: Ensure Vertex AI requests generate accurate cost calculations and usage tracking that integrates seamlessly with the existing statistics system.
Output: Complete usage tracking and cost calculation for Vertex AI requests with pricing service integration.
</objective>

<execution_context>
@/Users/liou/js_project/claude-relay-service/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liou/js_project/claude-relay-service/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-advanced-features-admin-interface/06-01-PLAN.md

# Existing pricing service patterns
@src/services/pricingService.js

# Usage capture patterns from other providers
@src/services/relay/bedrockRelayService.js
@src/services/relay/claudeRelayService.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Vertex AI model pricing support to pricing service</name>
  <files>src/services/pricingService.js, tests/pricingService.vertex.test.js</files>
  <behavior>
    - Test 1: getModelPricing recognizes Vertex AI Claude model names (claude-opus-4-6, claude-sonnet-4-6)
    - Test 2: calculateCost works correctly for Vertex AI usage objects with input/output tokens
    - Test 3: Cost calculation handles Vertex AI-specific pricing structure and regional variations
    - Test 4: Model name mapping handles Vertex AI format variations and endpoint-specific naming
  </behavior>
  <action>
    Extend pricingService to support Vertex AI Claude models with proper model name recognition and cost calculation.

    Key enhancements needed:
    - Add Vertex AI Claude model mapping in getModelPricing (claude-opus-4-6, claude-sonnet-4-6 from Project requirements)
    - Handle Vertex AI model name variations and regional prefixes
    - Ensure calculateCost works with Vertex AI usage format (input_tokens, output_tokens)
    - Support future model additions through standard pricing data structure
    - Maintain existing pricing patterns and fallback mechanisms

    Reference existing Bedrock model handling for multi-provider patterns. Follow pricing service architecture for model lookups and cost calculations.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="pricingService.*vertex"</automated>
  </verify>
  <done>Pricing service recognizes Vertex AI models and calculates costs accurately for Vertex AI usage data</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate usage capture and cost calculation in Vertex AI relay service</name>
  <files>src/services/relay/vertexRelayService.js, tests/vertexRelayService.usage.test.js</files>
  <behavior>
    - Test 1: Captures accurate input and output token counts from Vertex AI responses
    - Test 2: Calculates total cost using pricingService.calculateCost with captured usage
    - Test 3: Returns properly formatted usage object for statistics integration
    - Test 4: Handles both streaming and non-streaming usage capture consistently
    - Test 5: Integrates cost data with existing relay service return format
  </behavior>
  <action>
    Implement comprehensive usage capture and cost calculation in vertexRelayService, integrating with existing pricingService.

    Key integration points:
    - Capture usage from both streaming (accumulated during stream) and non-streaming responses
    - Format usage object: { input_tokens, output_tokens, model, duration }
    - Call pricingService.calculateCost(usage, modelName) for cost calculation
    - Return usage and cost data in standard relay service format: { success, usage, cost, model, duration }
    - Handle usage extraction from Vertex AI response format
    - Integrate with existing error handling and logging patterns
    - Follow dependency injection patterns for testability

    Reference bedrockRelayService usage capture patterns for consistency. Maintain Clean Architecture with pricing service as dependency.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="vertexRelayService.*usage"</automated>
  </verify>
  <done>Vertex AI relay service captures usage statistics accurately and integrates with pricing service for cost calculation</done>
</task>

</tasks>

<verification>
- Usage statistics are captured correctly from Vertex AI responses
- Cost calculations work accurately using existing pricing service
- Usage data format is consistent with other providers for statistics integration
- Both streaming and non-streaming requests generate proper usage data
</verification>

<success_criteria>
- Vertex AI usage statistics integrate seamlessly with existing pricing and statistics system
- Cost calculations are accurate based on Vertex AI model pricing
- Usage capture works for both streaming and non-streaming responses
- Integration maintains existing architecture patterns and error handling
</success_criteria>

<output>
After completion, create `.planning/phases/06-advanced-features-admin-interface/06-02-SUMMARY.md`
</output>