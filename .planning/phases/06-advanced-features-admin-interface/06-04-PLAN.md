---
phase: 06-advanced-features-admin-interface
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified: [src/routes/apiStats.js, src/services/statisticsService.js, web/admin-spa/src/utils/http_apis.js]
autonomous: true
requirements: [FEATURES-06]

must_haves:
  truths:
    - "Vertex AI usage data appears in existing statistics dashboards and reports"
    - "System tracks Vertex AI requests, tokens, and costs alongside other providers"
    - "Statistics API endpoints include Vertex AI data in aggregated responses"
  artifacts:
    - path: "src/routes/apiStats.js"
      provides: "Vertex AI statistics API endpoints"
      contains: "vertex.*stats"
      min_lines: 10
    - path: "src/services/statisticsService.js"
      provides: "Vertex AI statistics integration"
      contains: "vertex.*usage"
      min_lines: 20
    - path: "web/admin-spa/src/utils/http_apis.js"
      provides: "Vertex AI statistics HTTP API functions"
      contains: "VertexAi.*Stats.*Api"
      min_lines: 5
  key_links:
    - from: "apiStats.js"
      to: "statisticsService"
      via: "statistics service integration"
      pattern: "statisticsService\\."
    - from: "statisticsService"
      to: "redis"
      via: "usage data storage"
      pattern: "redis\\."
---

<objective>
Integrate Vertex AI usage tracking into existing statistics and reporting system.

Purpose: Ensure Vertex AI usage data is captured, stored, and displayed alongside other providers in the unified statistics dashboard.
Output: Complete integration of Vertex AI metrics into existing statistics infrastructure with API endpoints and data aggregation.
</objective>

<execution_context>
@/Users/liou/js_project/claude-relay-service/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liou/js_project/claude-relay-service/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-advanced-features-admin-interface/06-02-PLAN.md

# Existing statistics patterns
@src/routes/apiStats.js
@src/services/statisticsService.js
@web/admin-spa/src/utils/http_apis.js

# Statistics integration from other providers
@src/services/relay/bedrockRelayService.js
@src/services/relay/claudeRelayService.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend statistics service for Vertex AI usage tracking</name>
  <files>src/services/statisticsService.js, tests/statisticsService.vertex.test.js</files>
  <behavior>
    - Test 1: recordUsage accepts Vertex AI usage data and stores it properly
    - Test 2: getProviderStats includes Vertex AI data in aggregated statistics
    - Test 3: getUsageByTimeRange includes Vertex AI usage in time-based queries
    - Test 4: calculateTotalCosts includes Vertex AI costs in overall cost calculations
  </behavior>
  <action>
    Extend statisticsService to support Vertex AI usage tracking and aggregation with existing statistics infrastructure.

    Key enhancements needed:
    - Add Vertex AI provider support in recordUsage function
    - Include 'vertex_ai' in provider enumeration for statistics aggregation
    - Ensure getProviderStats includes Vertex AI metrics (requests, tokens, costs, errors)
    - Update getUsageByTimeRange to include Vertex AI data in time-based queries
    - Integrate Vertex AI costs in calculateTotalCosts aggregation
    - Maintain Redis key patterns for Vertex AI usage: `vertex_ai:usage:*`
    - Support Vertex AI-specific metrics (project_id, region usage distribution)

    Follow existing patterns from other providers (bedrock, claude, openai) for consistency. Ensure data structure compatibility with existing dashboard components.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="statisticsService.*vertex"</automated>
  </verify>
  <done>Statistics service captures and aggregates Vertex AI usage data alongside other providers</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Vertex AI statistics API endpoints and frontend integration</name>
  <files>src/routes/apiStats.js, web/admin-spa/src/utils/http_apis.js, tests/apiStats.vertex.test.js</files>
  <behavior>
    - Test 1: GET /admin/stats/vertex-ai returns Vertex AI usage statistics
    - Test 2: Statistics endpoints include Vertex AI data in provider aggregations
    - Test 3: Frontend HTTP API functions can fetch Vertex AI statistics data
    - Test 4: Time range filtering works correctly for Vertex AI usage queries
  </behavior>
  <action>
    Add Vertex AI statistics API endpoints and integrate with frontend HTTP API functions.

    Backend API endpoints (apiStats.js):
    - Add GET /admin/stats/vertex-ai for provider-specific statistics
    - Ensure existing aggregated endpoints (/admin/stats/overview) include Vertex AI data
    - Support time range filtering: ?startDate=YYYY-MM-DD&endDate=YYYY-MM-DD
    - Return structured data: { requests, tokens, costs, errors, topModels, usageByPeriod }
    - Include Vertex AI-specific metrics: project usage, regional distribution
    - Follow existing endpoint patterns and authentication middleware

    Frontend API integration (http_apis.js):
    - Add getVertexAiStatsApi(params) function
    - Follow existing statistics API function patterns
    - Support time range parameters and filtering options
    - Maintain consistent error handling and response formatting

    Integration ensures Vertex AI usage appears in existing dashboard components without requiring component changes.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="apiStats.*vertex"</automated>
  </verify>
  <done>API endpoints serve Vertex AI statistics and frontend can fetch Vertex AI usage data</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Vertex AI relay service usage reporting</name>
  <files>src/services/relay/vertexRelayService.js</files>
  <action>
    Complete the integration between Vertex AI relay service and statistics system by adding usage reporting calls.

    Integration points needed:
    - Import statisticsService in vertexRelayService
    - Call statisticsService.recordUsage after successful requests (both streaming and non-streaming)
    - Include all relevant data: provider='vertex_ai', model, usage, cost, duration, success status
    - Handle error cases and failed requests in statistics recording
    - Follow existing patterns from bedrockRelayService usage reporting
    - Maintain consistency with error handling and logging

    Usage data structure:
    ```javascript
    await statisticsService.recordUsage({
      provider: 'vertex_ai',
      model: modelName,
      usage: { input_tokens, output_tokens },
      cost: calculatedCost,
      duration: requestDuration,
      success: true,
      error: errorMessage // if applicable
    })
    ```

    This completes the usage tracking loop: relay service → statistics service → API endpoints → frontend dashboard.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=vertexRelayService && npm run lint</automated>
  </verify>
  <done>Vertex AI relay service reports usage to statistics system for complete tracking integration</done>
</task>

</tasks>

<verification>
- Vertex AI usage data flows from relay service to statistics system
- API endpoints serve Vertex AI statistics alongside other providers
- Frontend can fetch and display Vertex AI usage metrics
- Aggregated statistics include Vertex AI data properly
- Usage tracking works for both successful and failed requests
</verification>

<success_criteria>
- Vertex AI usage appears in existing statistics dashboards
- Statistics API endpoints include Vertex AI data in responses
- Usage tracking captures all relevant Vertex AI metrics
- Integration maintains existing statistics architecture patterns
- Vertex AI data is properly aggregated with other provider statistics
</success_criteria>

<output>
After completion, create `.planning/phases/06-advanced-features-admin-interface/06-04-SUMMARY.md`
</output>