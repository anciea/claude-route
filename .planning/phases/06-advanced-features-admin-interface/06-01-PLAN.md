---
phase: 06-advanced-features-admin-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/services/relay/vertexRelayService.js, src/utils/vertexStreamHandler.js]
autonomous: true
requirements: [FEATURES-01]
must_haves:
  truths:
    - "Vertex AI Claude models return streaming responses with real-time token counting"
    - "Streaming responses follow Claude SSE format with event types (message_start, content_block_start, content_block_delta, etc.)"
    - "Token usage is captured during streaming and accumulated for final usage statistics"
  artifacts:
    - path: "src/services/relay/vertexRelayService.js"
      provides: "Vertex AI streaming response handler"
      exports: ["stream"]
      min_lines: 50
    - path: "src/utils/vertexStreamHandler.js"
      provides: "Vertex AI to Claude format stream conversion"
      exports: ["convertVertexStreamToClaudeFormat"]
      min_lines: 100
  key_links:
    - from: "src/services/relay/vertexRelayService.js"
      to: "src/utils/vertexStreamHandler.js"
      via: "stream conversion import"
      pattern: "require.*vertexStreamHandler"
    - from: "vertexStreamHandler"
      to: "res.write"
      via: "SSE event streaming"
      pattern: "res\\.write.*event.*data"
---

<objective>
Enable streaming response support for Vertex AI Claude models with real-time usage token capture.

Purpose: Provide feature parity with other providers for streaming responses while maintaining unified Claude API format and accurate usage tracking.
Output: Vertex AI relay service with complete streaming support that captures tokens as they're generated.
</objective>

<execution_context>
@/Users/liou/js_project/claude-relay-service/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liou/js_project/claude-relay-service/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/phases/04-vertex-ai-account-foundation/04-01-SUMMARY.md
@.planning/phases/04-vertex-ai-account-foundation/04-02-SUMMARY.md

# Streaming patterns from existing providers
@src/services/relay/bedrockRelayService.js
@src/services/relay/claudeRelayService.js

# Vertex AI account service integration
@src/services/account/vertexAiAccountService.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Vertex AI to Claude stream format converter</name>
  <files>src/utils/vertexStreamHandler.js, src/utils/vertexStreamHandler.test.js</files>
  <behavior>
    - Test 1: Converts Vertex AI streaming chunks to Claude SSE format (message_start, content_block_start, content_block_delta, message_delta)
    - Test 2: Accumulates usage tokens from streaming response chunks (input_tokens, output_tokens)
    - Test 3: Handles Vertex AI specific streaming events and maps to Claude API equivalents
    - Test 4: Preserves content ordering and maintains response structure consistency
    - Test 5: Extracts and aggregates usage statistics from final stream chunks
  </behavior>
  <action>
    Create utility module that converts Google Cloud Vertex AI streaming response format to unified Claude SSE format per D-01 (follow existing streaming patterns), D-02 (implement SSE), and D-03 (support Vertex AI streaming format conversion).

    Key conversions needed:
    - Map Vertex AI stream events to Claude event types (message_start, content_block_start, content_block_delta, content_block_stop, message_delta, message_stop)
    - Extract and aggregate token usage during streaming per D-05 (capture usage statistics in real-time)
    - Maintain content ordering and response structure
    - Handle error events and conversion edge cases
    - Return consistent SSE format: `event: ${type}\ndata: ${JSON.stringify(data)}\n\n`

    Reference Bedrock stream handler patterns for consistent architecture. Export `convertVertexStreamToClaudeFormat(vertexChunk, isFirstChunk)` function. Implementation follows established patterns per D-01.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=vertexStreamHandler</automated>
  </verify>
  <done>Vertex AI streaming chunks convert correctly to Claude SSE format with accurate token accumulation and proper event sequencing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Vertex AI relay streaming response handler</name>
  <files>src/services/relay/vertexRelayService.js, src/services/relay/vertexRelayService.test.js</files>
  <behavior>
    - Test 1: Handles streaming requests by setting SSE headers and processing Vertex AI stream chunks
    - Test 2: Integrates with vertexStreamHandler to convert stream format in real-time
    - Test 3: Captures total usage statistics from streaming response for cost calculation
    - Test 4: Returns usage object with input_tokens, output_tokens, and timing metrics
    - Test 5: Gracefully handles stream interruption and client disconnection with AbortController
  </behavior>
  <action>
    Implement streaming response handling in vertexRelayService following D-01 (established patterns), D-02 (SSE for real-time streaming), D-04 (AbortController cleanup), and D-05 (real-time usage capture).

    Key features needed:
    - Set proper SSE headers: 'Content-Type': 'text/event-stream', 'Cache-Control': 'no-cache', Connection: 'keep-alive' per D-02
    - Process Vertex AI streaming response chunks using Google Cloud client library
    - Convert each chunk using vertexStreamHandler utility per D-03
    - Write SSE events: `res.write('event: ${type}\ndata: ${JSON.stringify(data)}\n\n')`
    - Accumulate usage statistics during streaming per D-05
    - Handle AbortController for client disconnection cleanup per D-04
    - Send completion events: 'event: done\ndata: [DONE]\n\n'
    - Return usage object for cost calculation integration

    Follow Clean Architecture and existing patterns per D-01: relay service calls stream handler utility, handles HTTP concerns, manages response lifecycle.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=vertexRelayService</automated>
  </verify>
  <done>Vertex AI relay service supports streaming with proper SSE formatting, usage capture, and client disconnection handling</done>
</task>

</tasks>

<verification>
- Vertex AI streaming responses work correctly with proper SSE format
- Usage tokens are captured accurately during streaming
- Stream conversion maintains Claude API compatibility
- AbortController properly handles client disconnections
</verification>

<success_criteria>
- Streaming responses from Vertex AI models work with real-time token capture
- Response format matches existing Claude API SSE specifications
- Usage statistics integrate properly with existing cost calculation system
- Clean Architecture maintained with proper separation between relay service and stream utilities
</success_criteria>

<output>
After completion, create `.planning/phases/06-advanced-features-admin-interface/06-01-SUMMARY.md`
</output>