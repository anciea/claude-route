---
phase: 06-advanced-features-admin-interface
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified: [web/admin-spa/src/components/VertexAiAccountManagement.vue, web/admin-spa/src/views/Accounts/VertexAi.vue]
autonomous: false
requirements: [FEATURES-04]

must_haves:
  truths:
    - "Admin interface displays Vertex AI accounts with status, usage, and health indicators"
    - "Admin can view Vertex AI account details including Project ID, region, and connection status"
    - "Admin can monitor Vertex AI usage statistics and account health metrics"
  artifacts:
    - path: "web/admin-spa/src/components/VertexAiAccountManagement.vue"
      provides: "Vertex AI account management component"
      contains: "template.*VertexAi.*account"
      min_lines: 100
    - path: "web/admin-spa/src/views/Accounts/VertexAi.vue"
      provides: "Vertex AI accounts page view"
      contains: "VertexAiAccountManagement"
      min_lines: 50
  key_links:
    - from: "VertexAi.vue"
      to: "VertexAiAccountManagement.vue"
      via: "component import and usage"
      pattern: "import.*VertexAiAccountManagement"
    - from: "VertexAiAccountManagement.vue"
      to: "accounts store"
      via: "Pinia store integration"
      pattern: "useAccountsStore"
---

<objective>
Create comprehensive admin interface for Vertex AI account management and monitoring.

Purpose: Provide admins with complete visibility into Vertex AI account status, usage statistics, and health metrics through an intuitive web interface.
Output: Vue components and views that enable full Vertex AI account management with monitoring capabilities.
</objective>

<execution_context>
@/Users/liou/js_project/claude-relay-service/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liou/js_project/claude-relay-service/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-vertex-ai-account-foundation/04-02-SUMMARY.md

# Existing admin interface patterns
@web/admin-spa/src/components/ClaudeAccountManagement.vue
@web/admin-spa/src/views/Accounts/Claude.vue
@web/admin-spa/src/stores/accounts.js
@web/admin-spa/src/stores/stats.js

# Project UI requirements
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vertex AI account management component</name>
  <files>web/admin-spa/src/components/VertexAiAccountManagement.vue</files>
  <action>
    Create comprehensive Vue component for Vertex AI account management following established patterns from other provider account components, implementing decisions D-16 through D-20 from CONTEXT.md.

    Component features per D-16 (add Vertex AI account monitoring):
    - Account list table with columns: Name, Project ID, Region, Status, Usage, Actions
    - Create account form with fields: name, projectId, region, serviceAccountJson (file upload), accountGroup
    - Edit account modal with updateable fields (name, projectId, region, accountGroup)
    - Delete account confirmation with proper warnings
    - Test connection functionality with status feedback
    - Account status toggle (active/inactive) with immediate UI feedback

    Usage monitoring per D-17 (display account health status, usage metrics):
    - Usage statistics display (requests, tokens, costs) with time period filtering
    - Health status indicators (connection status, error count, last successful request)
    - Real-time usage statistics per D-20

    Design requirements per D-18 and D-19 (follow existing patterns, dark mode, responsive):
    - Responsive design with Tailwind CSS and dark mode support per CLAUDE.md requirements
    - Element Plus UI components for consistency with existing interface
    - Follow Vue 3 Composition API patterns per D-18
    - Maintain glass theme design consistency per D-19
    - Proper error handling and loading states throughout
    - Implement proper accessibility features

    Integration points:
    - Use useAccountsStore for all account operations
    - Use useStatsStore for usage statistics
    - Follow exact patterns from ClaudeAccountManagement.vue for consistency
  </action>
  <verify>
    <automated>npm run lint && npm run build:web</automated>
  </verify>
  <done>Vertex AI account management component provides complete CRUD operations with usage monitoring and status management</done>
</task>

<task type="auto">
  <name>Task 2: Create Vertex AI accounts page view</name>
  <files>web/admin-spa/src/views/Accounts/VertexAi.vue</files>
  <action>
    Create Vertex AI accounts page view that wraps the account management component following established admin interface patterns.

    View implementation:
    - Simple page wrapper that imports and uses VertexAiAccountManagement component
    - Proper page title and metadata
    - Breadcrumb navigation integration
    - Loading and error states handled at page level
    - Follow existing account view patterns from other providers

    Maintain consistency with existing provider patterns and dark mode theming per CLAUDE.md requirements and design decisions D-18, D-19.
  </action>
  <verify>
    <automated>npm run lint && npm run build:web</automated>
  </verify>
  <done>Vertex AI accounts page provides proper page wrapper for account management component</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Vertex AI admin interface</name>
  <what-built>Complete Vertex AI admin interface with account management and usage monitoring</what-built>
  <how-to-verify>
    1. Navigate to admin interface at http://localhost:5003/admin-next/
    2. Click on "Accounts" section and verify "Vertex AI" option appears
    3. Navigate to Vertex AI accounts page (/admin/accounts/vertex-ai)
    4. Verify account management interface displays correctly:
       - Account list table with proper columns
       - Create account form with Service Account JSON upload
       - Account status indicators and action buttons
       - Usage statistics section with time period filters
    5. Test responsiveness by resizing browser window
    6. Toggle dark mode and verify all components render correctly
    7. Verify glassmorphism theme consistency with other provider pages
  </how-to-verify>
  <resume-signal>Type "approved" if interface looks correct and follows established design patterns, or describe specific issues</resume-signal>
</task>

</tasks>

<verification>
- Admin interface displays comprehensive Vertex AI account information
- Account management operations work through Vue components
- Usage statistics and health monitoring integrate properly
- Design follows established patterns with dark mode support
- Navigation and routing work correctly
</verification>

<success_criteria>
- Admin can view and manage Vertex AI accounts through web interface
- Account health and usage metrics are displayed clearly
- Interface maintains design consistency with other provider pages
- All account operations work through the web interface
- Statistics integration provides meaningful usage insights
</success_criteria>

<output>
After completion, create `.planning/phases/06-advanced-features-admin-interface/06-03-SUMMARY.md`
</output>