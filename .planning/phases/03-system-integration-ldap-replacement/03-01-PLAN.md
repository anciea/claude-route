---
phase: 03-system-integration-ldap-replacement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/userRoutes.js
  - src/middleware/auth.js
  - src/routes/admin/system.js
  - config/config.example.js
  - package.json
  - src/services/ldapService.js
autonomous: true
requirements: [SYS-01, SYS-02, SYS-03]
must_haves:
  truths:
    - "LDAP login endpoint POST /users/login no longer exists"
    - "LDAP test endpoint GET /users/admin/ldap-test no longer exists"
    - "ldapService.js is deleted from the codebase"
    - "ldapjs npm package is removed from dependencies"
    - "Auth middleware defaults authMethod to 'session' instead of 'ldap'"
    - "OEM settings returns googleOAuthEnabled instead of ldapEnabled"
    - "All existing non-LDAP routes in userRoutes.js still function correctly"
    - "Existing API key validation middleware remains completely untouched"
  artifacts:
    - path: "src/routes/userRoutes.js"
      provides: "User routes without LDAP login endpoint"
      contains: "no ldapService import"
    - path: "src/middleware/auth.js"
      provides: "Auth middleware with updated defaults"
      contains: "authMethod: session.authMethod || 'session'"
    - path: "src/routes/admin/system.js"
      provides: "OEM settings with googleOAuthEnabled flag"
      contains: "googleOAuthEnabled"
  key_links:
    - from: "src/routes/userRoutes.js"
      to: "src/services/userService.js"
      via: "still imports userService for profile, logout, api-keys"
      pattern: "userService"
    - from: "src/routes/admin/system.js"
      to: "config/config.js"
      via: "reads googleOAuth config for enabled flag"
      pattern: "config\\.googleOAuth"
---

<objective>
Remove all LDAP authentication infrastructure from the backend and update system configuration to reflect Google OAuth2 as the sole authentication method.

Purpose: Complete SYS-01 (replace LDAP endpoints), SYS-02 (session management update), and ensure SYS-03 (existing API key validation unaffected) by surgically removing LDAP code while preserving all other user management functionality.

Output: Clean backend with no LDAP dependencies, updated auth middleware defaults, and OEM settings reflecting Google OAuth2 status.
</objective>

<execution_context>
@/Users/liou/js_project/claude-relay-service/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liou/js_project/claude-relay-service/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-google-oauth2-backend-foundation/01-03-SUMMARY.md
@.planning/phases/02-user-management-api-key-automation/02-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs from existing codebase -->

From src/routes/userRoutes.js (LDAP-dependent sections to REMOVE):
- Line 3: `const ldapService = require('../services/ldapService')` -- REMOVE
- Lines 52-161: `router.post('/login', ...)` -- REMOVE entire handler
- Lines 117-124: LDAP enabled check -- part of login handler, REMOVE with it
- Lines 746-762: `router.get('/admin/ldap-test', ...)` -- REMOVE entire handler

From src/routes/userRoutes.js (sections to KEEP unchanged):
- Lines 164-181: `router.post('/logout', ...)` -- KEEP
- Lines 184-222: `router.get('/profile', ...)` -- KEEP
- Lines 225-287: `router.get('/api-keys', ...)` -- KEEP
- Lines 290-366: `router.post('/api-keys', ...)` -- KEEP
- Lines 369-410: `router.delete('/api-keys/:keyId', ...)` -- KEEP
- Lines 413-449: `router.get('/usage-stats', ...)` -- KEEP
- Lines 454-496: `router.get('/', ...)` (admin user list) -- KEEP
- Lines 499-558: `router.get('/:userId', ...)` (admin user detail) -- KEEP
- Lines 561-635: PATCH routes (status, role) -- KEEP
- Lines 638-667: `router.post('/:userId/disable-keys', ...)` -- KEEP
- Lines 670-743: Admin usage stats and overview -- KEEP
- Lines 766-925: Quota card routes -- KEEP

From src/middleware/auth.js:
- Line 1534: `authMethod: session.authMethod || 'ldap'` -- CHANGE default to 'session'
- Line 1624: `authMethod: session.authMethod || 'ldap'` -- CHANGE default to 'session'

From src/routes/admin/system.js:
- Line 292: `ldapEnabled: config.ldap && config.ldap.enabled === true` -- REPLACE with googleOAuthEnabled

From config/config.example.js (Google OAuth2 section already exists from Phase 1):
```javascript
googleOAuth: {
  enabled: process.env.GOOGLE_OAUTH_ENABLED === 'true',
  clientId: process.env.GOOGLE_OAUTH_CLIENT_ID || '',
  clientSecret: process.env.GOOGLE_OAUTH_CLIENT_SECRET || '',
  redirectUri: process.env.GOOGLE_OAUTH_REDIRECT_URI || '',
  allowedDomains: process.env.GOOGLE_OAUTH_ALLOWED_DOMAINS
    ? process.env.GOOGLE_OAUTH_ALLOWED_DOMAINS.split(',')
    : []
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove LDAP routes and service, update auth middleware defaults</name>
  <files>src/routes/userRoutes.js, src/middleware/auth.js, src/services/ldapService.js</files>
  <action>
**In src/routes/userRoutes.js (per SYS-01):**
1. Remove the import: `const ldapService = require('../services/ldapService')` (line 3)
2. Remove the entire LDAP login handler `router.post('/login', ...)` (lines 52-161). This includes the rate limiting setup specific to login, the LDAP enabled check, and the ldapService.authenticateUserCredentials call. KEEP the rate limiter initialization function and variables (lines 17-49) ONLY if they are used elsewhere -- check first. If the rate limiters (ipRateLimiter, strictIpRateLimiter) are ONLY used in the login handler, remove them too along with the `RateLimiterRedis` import.
3. Remove the LDAP test endpoint `router.get('/admin/ldap-test', ...)` (lines 746-762).
4. Verify all remaining routes compile and have no references to ldapService.
5. The `initRateLimiters` function, `ipRateLimiter`, `strictIpRateLimiter` variables, and the `RateLimiterRedis` import -- these are ONLY used within the login handler. Remove all of them (lines 9, 17-49).

**In src/middleware/auth.js (per SYS-02):**
1. Line 1534: Change `authMethod: session.authMethod || 'ldap'` to `authMethod: session.authMethod || 'session'`
2. Line 1624: Change `authMethod: session.authMethod || 'ldap'` to `authMethod: session.authMethod || 'session'`
3. Do NOT change any other middleware logic. The session validation, admin auth, and API key auth must remain untouched (per SYS-03).

**Delete src/services/ldapService.js (per SYS-01):**
Delete the entire file. It is only imported by userRoutes.js (which we just cleaned).

After all changes, run `npx prettier --write src/routes/userRoutes.js src/middleware/auth.js` to format.
  </action>
  <verify>
    <automated>cd /Users/liou/js_project/claude-relay-service && node -e "require('./src/routes/userRoutes')" && node -e "require('./src/middleware/auth')" && ! test -f src/services/ldapService.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- ldapService.js deleted
- userRoutes.js loads without ldapService import, no /login or /ldap-test routes
- auth.js defaults authMethod to 'session' instead of 'ldap'
- All remaining user routes (logout, profile, api-keys, admin endpoints, quota cards) unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Update OEM settings, config, and remove ldapjs dependency</name>
  <files>src/routes/admin/system.js, config/config.example.js, package.json</files>
  <action>
**In src/routes/admin/system.js (per SYS-01):**
1. Find line 292 where OEM settings response includes `ldapEnabled: config.ldap && config.ldap.enabled === true`
2. Replace with `googleOAuthEnabled: config.googleOAuth && config.googleOAuth.enabled === true`
3. Also add `userManagementEnabled: config.userManagement && config.userManagement.enabled === true` on the next line so the frontend knows if user management is available (it was previously gated on LDAP, but should now be gated on userManagement + googleOAuth).

**In config/config.example.js:**
1. Find the LDAP configuration section (lines 141-180, the `ldap: { ... }` block).
2. Comment out the entire LDAP block with a note: `// LDAP authentication has been replaced by Google OAuth2. See googleOAuth section above.`
3. Do NOT delete the section entirely -- keeping it commented preserves reference for anyone who had LDAP configured.

**In package.json:**
1. Remove `"ldapjs": "^3.0.7"` from the dependencies section.
2. Run `npm install` to update package-lock.json accordingly.

After changes, run `npx prettier --write src/routes/admin/system.js config/config.example.js`.
Run `npm run lint:check` to ensure no lint errors across the codebase.
Run `npm test` to ensure all existing tests pass (300+ tests). If any test fails due to ldapService references, fix the mock/import.
  </action>
  <verify>
    <automated>cd /Users/liou/js_project/claude-relay-service && npm run lint:check && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>
- OEM settings returns googleOAuthEnabled and userManagementEnabled instead of ldapEnabled
- config.example.js has LDAP section commented out with migration note
- ldapjs removed from package.json dependencies
- All linting passes
- All existing tests pass
  </done>
</task>

</tasks>

<verification>
1. `node -e "const r = require('./src/routes/userRoutes'); console.log('userRoutes OK')"` -- loads without error
2. `grep -r "ldapService" src/routes/ src/middleware/` -- returns no results
3. `grep -r "require.*ldapjs" src/` -- returns no results
4. `! test -f src/services/ldapService.js && echo "DELETED"` -- confirms deletion
5. `grep "googleOAuthEnabled" src/routes/admin/system.js` -- confirms new flag
6. `grep "session.authMethod || 'session'" src/middleware/auth.js | wc -l` -- returns 2
7. `npm test` -- all tests pass
8. `npm run lint:check` -- no errors
</verification>

<success_criteria>
- Zero references to ldapService in any source file under src/
- ldapjs not in package.json dependencies
- src/services/ldapService.js does not exist
- POST /users/login route removed
- GET /users/admin/ldap-test route removed
- Auth middleware defaults to 'session' for authMethod
- OEM settings endpoint returns googleOAuthEnabled
- All 300+ existing tests pass
- Lint check clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-system-integration-ldap-replacement/03-01-SUMMARY.md`
</output>
