---
phase: 03-system-integration-ldap-replacement
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - tests/authRoutes.test.js
  - tests/userRoutes.test.js
autonomous: false
requirements: [SYS-01, SYS-02, SYS-03, SYS-04, USER-05]
must_haves:
  truths:
    - "All existing tests pass after LDAP removal"
    - "New tests verify LDAP routes are gone (404 on POST /users/login)"
    - "New tests verify OAuth2 callback redirects to frontend"
    - "API key authentication middleware works unchanged"
    - "Frontend builds successfully with no errors"
    - "End-to-end authentication flow is verified by human"
  artifacts:
    - path: "tests/userRoutes.test.js"
      provides: "Tests verifying LDAP removal and remaining routes"
      contains: "test.*login.*404\\|removed"
    - path: "tests/authRoutes.test.js"
      provides: "Updated tests for redirect-based callback"
      contains: "redirect"
  key_links:
    - from: "tests/authRoutes.test.js"
      to: "src/routes/authRoutes.js"
      via: "test assertions on redirect behavior"
      pattern: "redirect.*user-login"
    - from: "tests/userRoutes.test.js"
      to: "src/routes/userRoutes.js"
      via: "test assertions on removed LDAP routes"
      pattern: "login.*removed\\|404"
---

<objective>
Validate the complete LDAP-to-Google-OAuth2 migration through automated tests, build verification, and human end-to-end testing.

Purpose: Ensure all five Phase 3 requirements (SYS-01 through SYS-04, USER-05) are verifiably met by running the full test suite, adding regression tests for removed functionality, updating existing OAuth2 tests for the new redirect behavior, and performing human verification of the end-to-end flow.

Output: Updated test files, passing test suite, successful frontend build, and human-verified authentication flow.
</objective>

<execution_context>
@/Users/liou/js_project/claude-relay-service/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liou/js_project/claude-relay-service/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-system-integration-ldap-replacement/03-01-SUMMARY.md
@.planning/phases/03-system-integration-ldap-replacement/03-02-SUMMARY.md

<interfaces>
<!-- authRoutes.js after Plan 03-02 changes: callback now redirects instead of returning JSON -->
<!-- The test file needs to check for 302 redirect instead of 200 JSON response -->

<!-- userRoutes.js after Plan 03-01 changes: no /login or /ldap-test routes -->
<!-- New tests should verify these routes return 404 -->

<!-- Existing test patterns from tests/authRoutes.test.js: -->
```javascript
jest.mock('../src/services/googleOAuthService')
jest.mock('../src/services/userService')
jest.mock('../src/services/googleLoginService')
jest.mock('../src/models/redis')
jest.mock('../src/utils/logger')
jest.mock('../../config/config', () => ({ ... }))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update auth tests for redirect behavior and add LDAP removal regression tests</name>
  <files>tests/authRoutes.test.js, tests/userRoutes.test.js</files>
  <action>
**Update tests/authRoutes.test.js:**

The OAuth2 callback tests currently expect `res.json()` responses (200 status). After Plan 03-02, the callback redirects to the frontend. Update the relevant test cases:

1. Find the test for successful new user flow (the one checking for `apiKey` in response). Update it to:
   - Expect 302 status (redirect) instead of 200.
   - Expect `res.redirect()` was called with a URL containing `token=` and `user=` and `isNewUser=true` and `apiKey=cr_newkey` query params.
   - Use `expect(res.redirect).toHaveBeenCalledWith(expect.stringContaining('token='))` pattern.

2. Find the test for returning user flow. Update to expect 302 redirect with `isNewUser=false` and no `apiKey` param.

3. Find the test for API key generation failure. Update to expect 302 redirect with `apiKeyWarning=` param.

4. Error cases (missing code, invalid state, domain not allowed, disabled account) should still return JSON error responses (400, 403, etc.) -- these should NOT change. Verify they still pass as-is.

**Create tests/userRoutes.test.js:**

Create a new test file that verifies:

1. **LDAP login route removed**: Mock the router, send POST to /login on the userRoutes router. Verify it returns 404 (route not found) or the router has no handler for POST /login. Use supertest or direct router testing.
   ```javascript
   const express = require('express')
   const request = require('supertest')
   // Create minimal app with userRoutes
   const app = express()
   app.use(express.json())
   app.use('/users', require('../src/routes/userRoutes'))

   test('POST /users/login returns 404 (LDAP removed)', async () => {
     const res = await request(app).post('/users/login').send({ username: 'test', password: 'test' })
     expect(res.status).toBe(404)
   })
   ```
   Note: Since the route handler was removed, Express will return 404 by default. Mock all required dependencies (redis, userService, apiKeyService, etc.) that userRoutes still imports.

2. **LDAP test route removed**: Send GET to /users/admin/ldap-test. Verify 404.

3. **Existing routes still work**: Send GET to /users/profile with a valid mock session. Verify 200 response. This confirms SYS-03 (existing middleware unaffected).

4. **API key routes still work**: Send GET to /users/api-keys with mock auth. Verify 200 response.

Use jest.mock() for all service dependencies following the existing test patterns in the project. Mock `redis.getClientSafe()` to prevent actual Redis connections.

After writing tests:
- Run `npx prettier --write tests/authRoutes.test.js tests/userRoutes.test.js`
- Run `npm test` to verify all tests pass.
- Run `npm run lint:check` for clean lint.
  </action>
  <verify>
    <automated>cd /Users/liou/js_project/claude-relay-service && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>
- authRoutes.test.js updated for redirect-based callback (302 instead of 200)
- userRoutes.test.js created with LDAP removal regression tests
- POST /users/login confirmed to return 404
- GET /users/admin/ldap-test confirmed to return 404
- Existing routes (profile, api-keys) confirmed to still work
- All tests pass including existing 300+ tests
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend build verification and final lint pass</name>
  <files>web/admin-spa/src/views/UserLoginView.vue</files>
  <action>
Run the complete verification suite:

1. **Full backend test suite**: `npm test` -- all tests must pass.
2. **Backend lint**: `npm run lint:check` -- no errors.
3. **Frontend build**: `cd web/admin-spa && npm run build` -- must complete without errors.
4. **Verify no LDAP references remain**:
   - `grep -r "ldapService" src/` -- must return 0 results
   - `grep -r "ldapEnabled" web/admin-spa/src/` -- must return 0 results
   - `grep -r "require.*ldapjs" src/` -- must return 0 results
   - `test ! -f src/services/ldapService.js` -- must succeed
5. **Verify Google OAuth2 references exist**:
   - `grep "googleOAuthEnabled" src/routes/admin/system.js` -- must find it
   - `grep "/auth/google" web/admin-spa/src/views/UserLoginView.vue` -- must find it
   - `grep "handleGoogleCallback" web/admin-spa/src/stores/user.js` -- must find it
   - `grep "avatarUrl" web/admin-spa/src/views/UserManagementView.vue` -- must find it

If any check fails, diagnose and fix the issue. Do not proceed to the human verification checkpoint until all automated checks pass.
  </action>
  <verify>
    <automated>cd /Users/liou/js_project/claude-relay-service && npm test 2>&1 | grep -E "Tests:|Test Suites:" && npm run lint:check 2>&1 | tail -1 && cd web/admin-spa && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>
- All backend tests pass (300+)
- Lint check clean
- Frontend builds without errors
- Zero LDAP references in source code
- Google OAuth2 references present in all expected locations
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of end-to-end Google OAuth2 authentication flow</name>
  <files>none</files>
  <action>
Present the human with the complete verification checklist for the LDAP-to-Google-OAuth2 migration. All automated checks have already passed in Task 2. This checkpoint requires the human to verify the visual and functional aspects that cannot be automated.

What was built:
- Backend: LDAP service deleted, login/test routes removed, ldapjs uninstalled, auth middleware updated
- Frontend: Login page redirects to Google OAuth2, user management shows Google avatars and auth badges
- All ldapEnabled references replaced with googleOAuthEnabled/userManagementEnabled
- Test suite updated and passing

How to verify:
1. Start the dev server: `npm run dev` (backend) and `cd web/admin-spa and npm run dev` (frontend)
2. Visit the user login page at /user-login: verify "Sign in with Google" button (NOT username/password form)
3. Click the button: verify redirect to Google OAuth2 consent screen
4. Complete Google login: verify redirect back to /user-dashboard with session data
5. Check API Stats page at /api-stats: verify user login button visible
6. Login as admin and visit /user-management: verify Google avatars and auth method badges
7. Test API key functionality: verify existing keys still work, can create new keys
8. Verify no "LDAP" text visible anywhere in the UI
  </action>
  <verify>Human confirms all checks pass by typing "approved"</verify>
  <done>
- Human verified: Google OAuth2 login flow works end-to-end
- Human verified: User management displays Google user data (avatars, auth badges)
- Human verified: Existing API keys continue to function
- Human verified: No LDAP references visible in the UI
  </done>
</task>

</tasks>

<verification>
1. `npm test` -- all tests pass
2. `npm run lint:check` -- clean
3. `cd web/admin-spa && npm run build` -- succeeds
4. `grep -r "ldapService\|ldapEnabled\|require.*ldapjs" src/ web/admin-spa/src/ | wc -l` -- returns 0
5. Human verification of end-to-end Google OAuth2 flow
</verification>

<success_criteria>
- All automated tests pass (including new regression tests)
- Frontend builds without errors
- Zero LDAP references in entire codebase (src/ and web/admin-spa/src/)
- Human verifies: Google OAuth2 login works end-to-end
- Human verifies: User management shows Google user data correctly
- Human verifies: Existing API keys continue to function
</success_criteria>

<output>
After completion, create `.planning/phases/03-system-integration-ldap-replacement/03-03-SUMMARY.md`
</output>
