---
phase: 05-model-support-core-integration
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified: [src/routes/api.js, src/routes/unified.js, tests/integration/vertexAiRoutes.test.js]
autonomous: true
requirements: [INTEGRATION-01, INTEGRATION-03, INTEGRATION-05]

must_haves:
  truths:
    - "Clients can request Claude 4.6 models via unified Claude API endpoints"
    - "Vertex AI accounts are selectable through existing API route patterns"
    - "Authentication errors are handled gracefully with meaningful responses"
    - "Streaming and non-streaming requests work through existing endpoints"
    - "Integration follows established multi-provider routing patterns"
  artifacts:
    - path: "src/routes/api.js"
      provides: "Main Claude API route with Vertex AI support"
      contains: "vertex.*scheduler"
      min_lines: 5
    - path: "src/routes/unified.js"
      provides: "Unified API route with Vertex AI support"
      contains: "vertex.*relay"
      min_lines: 5
    - path: "tests/integration/vertexAiRoutes.test.js"
      provides: "Integration tests for Vertex AI routing"
      contains: "Claude 4.6.*model"
  key_links:
    - from: "src/routes/api.js"
      to: "unifiedVertexScheduler"
      via: "account selection for Claude API"
      pattern: "unifiedVertexScheduler\\.selectAccountForApiKey"
    - from: "src/routes/unified.js"
      to: "vertexRelayService"
      via: "request processing for Vertex AI"
      pattern: "vertexRelayService\\.sendVertexRequest"
    - from: "route handlers"
      to: "vertex-ai account type"
      via: "account type condition"
      pattern: "accountType.*vertex"
---

<objective>
Integrate Vertex AI relay service and scheduler into existing Claude API routes to enable client access to Claude 4.6 models.

Purpose: Complete the integration by wiring Vertex AI services into established API endpoints, enabling clients to access Claude models through Vertex AI accounts using unified Claude API format per D-05.
Output: Full integration with existing routes supporting Vertex AI account selection and request processing.
</objective>

<execution_context>
@/Users/liou/js_project/claude-relay-service/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liou/js_project/claude-relay-service/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-model-support-core-integration/05-CONTEXT.md

# Created in prior plans (Wave 1)
@.planning/phases/05-model-support-core-integration/05-01-PLAN.md
@.planning/phases/05-model-support-core-integration/05-02-PLAN.md

# Existing routing patterns
@src/routes/api.js
@src/routes/unified.js

# Multi-provider integration examples
@src/services/scheduler/unifiedGeminiScheduler.js
@src/services/relay/geminiRelayService.js
@src/middleware/auth.js
</context>

<interfaces>
<!-- Integration points from prior plans -->

From 05-01-PLAN.md (vertexRelayService):
```javascript
// Relay service interface (will be created in Wave 1)
async function sendVertexRequest({
  messages, model, temperature, maxTokens, stream,
  accessToken, proxy, apiKeyId, signal, projectId, location, accountId
})
```

From 05-02-PLAN.md (unifiedVertexScheduler):
```javascript
// Scheduler interface (will be created in Wave 1)
class UnifiedVertexScheduler {
  async selectAccountForApiKey(apiKeyData, sessionHash, requestedModel, options)
  async markAccountRateLimited(accountId, accountType, sessionHash)
}
```

From src/routes/api.js (existing patterns):
```javascript
// Existing multi-provider pattern in Claude API routes
if (claudeAccountResult.accountType === 'gemini') {
  // Handle Gemini account routing
} else if (claudeAccountResult.accountType === 'openai') {
  // Handle OpenAI account routing
}
// Need to add: else if (claudeAccountResult.accountType === 'vertex-ai')
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Vertex AI scheduler into main Claude API route</name>
  <files>src/routes/api.js</files>
  <action>
    Add Vertex AI support to the main Claude API route (/v1/messages) in api.js. Import unifiedVertexScheduler and integrate it into the existing account selection logic following established patterns from other providers. Add condition to check for vertex-ai account type and call unifiedVertexScheduler.selectAccountForApiKey when API key has vertexAccountId binding or when selecting from available accounts. Follow existing error handling patterns for account selection failures and maintain consistent response formats. Ensure integration follows D-10 for sticky session system and D-12 for unified scheduler architecture.
  </action>
  <verify>
    <automated>npm run lint && grep -n "unifiedVertexScheduler\|vertex.*scheduler" src/routes/api.js</automated>
  </verify>
  <done>Main Claude API route includes Vertex AI scheduler integration with account selection following established multi-provider patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Vertex AI relay service into unified routes</name>
  <files>src/routes/unified.js</files>
  <action>
    Add Vertex AI request processing to unified routes. Import vertexRelayService and add routing logic to handle accountType 'vertex-ai' in the unified API endpoint. Integrate sendVertexRequest function calls with proper parameter mapping (accessToken from account credentials, projectId/location from account config). Add error handling for Google Cloud authentication errors per D-15 and API errors per D-16. Include support for both streaming and non-streaming requests following existing patterns. Maintain AbortController integration for client disconnection cleanup per D-22.
  </action>
  <verify>
    <automated>npm run lint && grep -n "vertexRelayService\|vertex.*relay" src/routes/unified.js</automated>
  </verify>
  <done>Unified API routes include Vertex AI relay service integration with request processing and error handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create integration tests for Vertex AI route functionality</name>
  <files>tests/integration/vertexAiRoutes.test.js</files>
  <behavior>
    - Test Claude API endpoint accepts Claude 4.6 model requests
    - Verify Vertex AI account selection through unified scheduler
    - Test request/response format conversion through relay service
    - Verify streaming response handling works correctly
    - Test authentication error handling and response formatting
    - Verify sticky session behavior with Vertex AI accounts
  </behavior>
  <action>
    Create integration tests covering Vertex AI route functionality. Mock vertexAiAccountService, unifiedVertexScheduler, and vertexRelayService to test routing integration without external dependencies. Test requests to /v1/messages with claude-opus-4-6 and claude-sonnet-4-6 models, verify account selection logic, request format conversion, and response handling. Include tests for error scenarios (authentication failures, account unavailability) and streaming response processing. Follow existing integration test patterns from other provider tests.
  </action>
  <verify>
    <automated>npm test -- --testNamePattern="vertex.*route|route.*vertex" --testPathPattern="tests/integration/vertexAiRoutes.test.js"</automated>
  </verify>
  <done>Integration tests verify Vertex AI route functionality including model support, account selection, and error handling.</done>
</task>

</tasks>

<verification>
Route integration completeness checks:
- Main Claude API route supports Vertex AI account selection
- Unified routes handle Vertex AI request processing
- Error handling covers authentication and API failures
- Integration tests verify end-to-end functionality
- All multi-provider routing patterns maintained
</verification>

<success_criteria>
- Clients can request Claude 4.6 models via existing /v1/messages endpoint
- Vertex AI accounts integrate seamlessly with existing routing
- Authentication and API errors handled gracefully
- Streaming and non-streaming requests work correctly
- Integration maintains existing multi-provider patterns
</success_criteria>

<output>
After completion, create `.planning/phases/05-model-support-core-integration/05-03-SUMMARY.md`
</output>