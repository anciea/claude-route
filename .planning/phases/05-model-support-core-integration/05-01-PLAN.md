---
phase: 05-model-support-core-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/services/relay/vertexRelayService.js, tests/vertexRelayService.test.js]
autonomous: true
requirements: [MODELS-01, MODELS-02, MODELS-03, MODELS-04, INTEGRATION-04, INTEGRATION-05, INTEGRATION-06]

must_haves:
  truths:
    - "System converts Claude API requests to Vertex AI Partner Model format"
    - "Claude 4.6 Opus and Sonnet models are supported via model name mapping"
    - "Model parameters (temperature, max_tokens) are converted correctly"
    - "Vertex AI API errors are handled and converted to Claude format"
    - "Streaming responses work through Vertex AI format conversion"
  artifacts:
    - path: "src/services/relay/vertexRelayService.js"
      provides: "Vertex AI request/response conversion service"
      exports: ["sendVertexRequest", "convertClaudeToVertex", "convertVertexResponse", "handleVertexStream"]
      min_lines: 200
    - path: "tests/vertexRelayService.test.js"
      provides: "Unit tests for format conversion and streaming"
      contains: "Claude API to Vertex AI format conversion"
  key_links:
    - from: "src/services/relay/vertexRelayService.js"
      to: "google-auth-library"
      via: "access token authentication"
      pattern: "Bearer.*accessToken"
    - from: "sendVertexRequest function"
      to: "Vertex AI Partner Model API"
      via: "HTTPS POST request"
      pattern: "projects/.*/locations/.*/publishers/anthropic/models/.*:rawPredict"
    - from: "convertClaudeToVertex function"
      to: "Claude API message format"
      via: "message array transformation"
      pattern: "anthropic_version.*vertex-2023-10-16"
---

<objective>
Create Vertex AI relay service that converts Claude API requests to Vertex AI Partner Model format and handles streaming responses.

Purpose: Enable Vertex AI accounts to serve Claude 4.6 models through unified Claude API format while maintaining feature parity with other providers.
Output: Complete relay service for Vertex AI with request/response format conversion, streaming support, and comprehensive error handling.
</objective>

<execution_context>
@/Users/liou/js_project/claude-relay-service/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liou/js_project/claude-relay-service/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/phases/05-model-support-core-integration/05-CONTEXT.md

# Prior Phase Foundations
@.planning/phases/04-vertex-ai-account-foundation/04-01-SUMMARY.md
@.planning/phases/04-vertex-ai-account-foundation/04-02-SUMMARY.md

# Existing Relay Service Patterns
@src/services/relay/geminiRelayService.js
@src/services/relay/claudeRelayService.js
@src/services/account/vertexAiAccountService.js

# Request Processing Infrastructure
@src/utils/proxyHelper.js
@src/utils/logger.js
@config/config.js
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/services/account/vertexAiAccountService.js:
```javascript
// Account selection and authentication
async function selectAvailableAccount(sessionId)
async function getAccessToken(accountId)
async function getAccount(accountId, includeCredentials = false)
```

From src/services/relay/geminiRelayService.js:
```javascript
// Standard relay service interface pattern
async function sendGeminiRequest({
  messages, model, temperature, maxTokens, stream,
  accessToken, proxy, apiKeyId, signal, accountId
})
async function* handleStreamResponse(response, model, apiKeyId, accountId)
function convertMessagesToGemini(messages)
function convertGeminiResponse(geminiResponse, model, stream)
```

From config/config.js:
```javascript
const config = {
  requestTimeout: 600000,
  claude: {
    apiVersion: "2023-06-01",
    betaHeader: "...",
    systemPrompt: "..."
  }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Vertex AI format conversion functions</name>
  <files>src/services/relay/vertexRelayService.js, tests/vertexRelayService.test.js</files>
  <behavior>
    - convertClaudeToVertex: transforms Claude API message format to Vertex AI Partner Model format
    - Maps claude-opus-4-6 and claude-sonnet-4-6 model names correctly
    - Converts temperature, max_tokens parameters to Vertex AI format
    - Adds anthropic_version: vertex-2023-10-16 header requirement
    - convertVertexResponse: transforms Vertex AI response back to Claude format
    - Handles both streaming and non-streaming response formats
  </behavior>
  <action>
    Create core format conversion functions following established patterns from geminiRelayService.js. Implement convertClaudeToVertex that transforms Claude API message arrays to Vertex AI Partner Model format with proper model name mapping (claude-opus-4-6, claude-sonnet-4-6) and parameter conversion (temperature, max_tokens). Add convertVertexResponse for response format conversion back to Claude API format. Include anthropic_version: vertex-2023-10-16 header as required per D-09. Use TDD approach - write tests first, then implement functions to pass tests.
  </action>
  <verify>
    <automated>npm test -- --testNamePattern="vertexRelayService" --testPathPattern="tests/vertexRelayService.test.js"</automated>
  </verify>
  <done>Format conversion functions exist with passing unit tests for message transformation, model mapping, and response conversion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Vertex AI streaming response handler</name>
  <files>src/services/relay/vertexRelayService.js, tests/vertexRelayService.test.js</files>
  <behavior>
    - handleVertexStream: async generator function that processes streaming responses
    - Parses Vertex AI streaming format and converts to Claude API SSE format
    - Extracts usage statistics (input/output tokens) from stream events
    - Handles stream completion and cleanup properly
    - Integrates with AbortController for client disconnection
    - Records usage via apiKeyService.recordUsage following existing patterns
  </behavior>
  <action>
    Implement handleVertexStream async generator function following handleStreamResponse pattern from geminiRelayService.js. Process Vertex AI streaming responses, convert each chunk to Claude API format, handle usage statistics extraction, and integrate with existing AbortController and usage recording infrastructure. Use existing apiKeyService.recordUsage pattern with accountType 'vertex-ai'. Include proper error handling for stream interruption and client disconnection per D-22.
  </action>
  <verify>
    <automated>npm test -- --testNamePattern="handleVertexStream" --testPathPattern="tests/vertexRelayService.test.js"</automated>
  </verify>
  <done>Streaming handler function exists with tests covering stream processing, usage extraction, and error handling.</done>
</task>

<task type="auto">
  <name>Task 3: Implement main sendVertexRequest function with authentication and error handling</name>
  <files>src/services/relay/vertexRelayService.js</files>
  <action>
    Create sendVertexRequest main function following sendGeminiRequest pattern. Integrate with vertexAiAccountService for access token authentication, build Vertex AI Partner Model API endpoints using pattern: projects/{project}/locations/{location}/publishers/anthropic/models/{model}:rawPredict. Add comprehensive error handling for Google Cloud authentication errors with exponential backoff per D-15, convert Vertex AI API errors to Claude format per D-16, and support HTTP 429/5XX error patterns per D-17. Include proxy support via ProxyHelper, timeout configuration, and AbortController signal support per D-22. Export all functions: sendVertexRequest, convertClaudeToVertex, convertVertexResponse, handleVertexStream.
  </action>
  <verify>
    <automated>npm run lint && npm test -- --testNamePattern="sendVertexRequest" --testPathPattern="tests/vertexRelayService.test.js"</automated>
  </verify>
  <done>Main request function implements Vertex AI authentication, error handling, and integrates with format conversion functions. All exports available and lint passes.</done>
</task>

</tasks>

<verification>
Overall phase checks:
- vertexRelayService.js exports match interface pattern from other relay services
- Format conversion preserves Claude API compatibility
- Streaming responses maintain real-time characteristics
- Error handling covers authentication and API failure scenarios
- Tests verify conversion accuracy and edge cases
</verification>

<success_criteria>
- Vertex AI relay service converts Claude API requests to Partner Model format correctly
- Claude 4.6 Opus and Sonnet models accessible via model name mapping
- Streaming responses work with usage statistics capture
- Authentication and error handling follow established patterns
- Unit tests verify format conversion accuracy and stream processing
</success_criteria>

<output>
After completion, create `.planning/phases/05-model-support-core-integration/05-01-SUMMARY.md`
</output>